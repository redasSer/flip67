# Copy this file to .env and fill in the values
# cp .env.example .env

# PostgreSQL
POSTGRES_DB=flip67
POSTGRES_USER=flip67
POSTGRES_PASSWORD=change_me_in_production

# App
SERVER_PORT=8080
DB_POOL_SIZE=10
JPA_SHOW_SQL=false

# Logging (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO
APP_LOG_LEVEL=INFO

